/**
 * Facilitator (game master) console — placeholder.
 *
 * Phase 3 of the roadmap builds this out: create a session from a template,
 * invite teams, advance years, and preview/trigger interventions. For now
 * this just confirms the route and layout are wired up.
 */
export default function FacilitatorPage() {
  return (
    <main className="min-h-screen p-8">
      <h1 className="text-2xl font-semibold mb-2">Facilitator console</h1>
      <p className="text-slate-300">
        Coming in Phase 3: create a session, advance years, and trigger events
        for all teams.
      </p>
    </main>
  );
}
